<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunny.test">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="asdf"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- <activity android:name=".activity.MainActivity" /> -->
        <!-- <activity android:name=".activity.DownloadManagerTest" /> -->


        <!-- <service android:name=".internet.FileDownloadService" /> -->


        <!-- <activity android:name=".activity.PopupWindowTest" /> -->
        <!-- <activity android:name=".base.BaseListActivity"> -->


        <!-- </activity> -->
        <!-- <activity android:name=".activity.MultiSelectTest"> -->
        <!-- <intent-filter> -->

        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity android:name=".activity.CustomViewStudyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>